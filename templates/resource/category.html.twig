{% extends "base.html.twig" %}
{% block title %}Catégorie{{ parent() }}{% endblock %}
{% block body %}
{% for category in categories %}
<section class = "category">
    <a href = "{{ path('resource_new_machineType', {'resource' : resource.id, 'category' : category.id}) }}"><img src = "{{ asset('/build/images/' ~ category.id ~ '.png') }}" style = "width: 200px; height:auto;"></a>
    <h2>{{ category.name}}</h2>
</section>
{% endfor %}
{% endblock %}